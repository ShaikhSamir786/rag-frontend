export function SEO({ title, description }) {
    // Basic SEO placeholder since Next.js uses Metadata API usually
    return (
        <div style={{ display: 'none' }}>
            <meta name="title" content={title} />
            <meta name="description" content={description} />
        </div>
    );
}
